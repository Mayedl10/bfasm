#include <cstdint>
#include <string>
#include <vector>

#include "Token.hpp"
#include "Compiler.hpp"
#include "Error.hpp"

std::vector<CompilerError> Compiler::validate(std::vector<Token> tokens) {
    
}
